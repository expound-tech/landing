{
  "name": "archos-guide",
  "version": "0.0.1",
  "description": "Guide: Use Arch Linux for code.",
  "scripts": {
    "clean": "rimraf static",
    "gen": "eleventy --config=.eleventy.cjs",
    "gen:watch": "eleventy --config=.eleventy.cjs --watch",
    "serve": "wds --root-dir=static --node-resolve --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lit": "^2.3.1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.1.0",
    "@web/dev-server": "^0.1.34",
    "@webcomponents/webcomponentsjs": "^2.6.0",
    "guide-steps": "^0.0.2",
    "rimraf": "^3.0.2"
  }
}
